# Transfer Image

A React SVG component that displays a stylized illustration representing order transfer functionality, featuring multiple document or card elements with connecting arrows.

## Syntax

```jsx copy
<TransferImage width={280} height={181} />
```

## Description

The `TransferImage` component is an SVG illustration that visually represents the concept of transferring orders or documents between systems or users. It features document/card elements with connection arrows, providing a visual metaphor for the transfer process.

### Features:
- Pure SVG implementation ensuring sharp rendering at any scale
- Accepts standard SVG props for customization
- Uses a monochromatic color scheme with subtle gray tones
- Includes animated-like elements with arrows showing directional flow
- Optimized with clip paths for complex shape rendering

## Technical Details

### Dependencies

```javascript copy
import { SVGProps } from "react"
```

### Props

The component accepts all standard SVG element props through React's `SVGProps<SVGSVGElement>` type.

| Prop      | Type                    | Description                                 |
|-----------|-------------------------|---------------------------------------------|
| width     | number or string        | Width of the SVG (default: 280)             |
| height    | number or string        | Height of the SVG (default: 181)            |
| ...props  | SVGProps&lt;SVGSVGElement&gt; | Any other valid SVG element prop

### Component Structure

```javascript copy
import { SVGProps } from "react"

const TransferImage = (props: SVGProps<SVGSVGElement>) => (
  <svg
    width="280"
    height="181"
    viewBox="0 0 280 181"
    fill="none"
    xmlns="http://www.w3.org/2000/svg"
    {...props}
  >
    {/* SVG content */}
  </svg>
)

export default TransferImage
```

## Design Elements

### Document/Card Elements
The SVG contains multiple rectangular elements with rounded corners representing documents or cards:

```jsx copy
<rect
  x="0.00428286"
  y="-0.742904"
  width="33.5"
  height="65.5"
  rx="6.75"
  transform="matrix(0.865865 0.500278 -0.871576 0.490261 189.756 88.938)"
  fill="#D4D4D8"
  stroke="#52525B"
  strokeWidth="1.5"
/>
<rect
  x="0.00428286"
  y="-0.742904"
  width="33.5"
  height="65.5"
  rx="6.75"
  transform="matrix(0.865865 0.500278 -0.871576 0.490261 189.756 85.938)"
  fill="white"
  stroke="#52525B"
  strokeWidth="1.5"
/>
```

### Directional Arrows
The illustration includes directional arrow elements to show the flow of transfers:

```jsx copy
<g clip-path="url(#clip0_20786_38285)">
  <path
    d="M133.106 82.3025L140.49 82.345L140.514 78.1353"
    stroke="#A1A1AA"
    strokeWidth="1.5"
    strokeLinecap="round"
    strokeLinejoin="round"
  />
</g>
```

### Document Details
Small rectangular elements represent text or content within the documents:

```jsx copy
<rect
  width="12"
  height="3"
  rx="1.5"
  transform="matrix(0.865865 0.500278 -0.871576 0.490261 196.447 92.7925)"
  fill="#D4D4D8"
/>
```

### Loading Indicator
A circular loading indicator is represented by a series of paths with varying opacity:

```jsx copy
<path
  d="M180.579 107.642L179.126 108.459"
  stroke="#52525B"
  strokeWidth="1.5"
  strokeLinecap="round"
  strokeLinejoin="round"
/>
<path
  opacity="0.88"
  d="M182.305 110.046L180.257 110.034"
  stroke="#52525B"
  strokeWidth="1.5"
  strokeLinecap="round"
  strokeLinejoin="round"
/>
<!-- Additional paths with decreasing opacity -->
```

## Styling

- **Color Palette:**
  - Background: White (`#FFFFFF`)
  - Document backgrounds: White and light gray (`#D4D4D8`) 
  - Borders and outlines: Dark gray (`#52525B`)
  - Arrows and icons: Medium gray (`#A1A1AA`)
- **Design Techniques:**
  - Uses clip paths for complex shapes
  - Employs transparency for subtle effects
  - Implements matrix transformations for 3D-like perspective
  - Combines rounded rectangles with varying fills for layered appearance

## Clip Paths

The SVG uses multiple clip paths to create complex shapes, particularly for the directional arrows:

```jsx copy
<clipPath id="clip0_20786_38285">
  <rect
    width="12"
    height="12"
    fill="white"
    transform="matrix(0.865865 0.500278 -0.871576 0.490261 138.36 75.1509)"
  />
</clipPath>
```

## Examples

### Basic Usage

```jsx copy
import TransferImage from "./TransferImage"

export default function TransferRequestPage() {
  return (
    <div className="flex flex-col items-center justify-center p-8">
      <h1 className="text-2xl font-bold mb-4">Order Transfer Request</h1>
      <TransferImage />
      <p className="mt-4 text-center max-w-md">
        You've received a request to transfer an order.
        Please review the details below.
      </p>
    </div>
  )
}
```

### With Custom Dimensions

```jsx copy
<TransferImage width={400} height={260} className="my-8" />
```

## Accessibility

- The SVG is purely decorative and doesn't contain critical information
- For optimal accessibility:
  - Consider adding `role="img"` and `aria-label="Illustration of order transfer process"` when using this component
  - For purely decorative usage, add `aria-hidden="true"` to remove it from the accessibility tree

## Browser Support

The component works in modern browsers supporting:

- SVG elements with complex transformations
- CSS matrix transformations
- SVG clip paths
- React components

## Notes for Developers

- The SVG is optimized for the default dimensions of 280×181px
- When scaling, maintain the aspect ratio to preserve the design
- The component accepts all standard SVG props, allowing for customization of appearance and behavior
- Consider adding a title or description element if the SVG conveys important information